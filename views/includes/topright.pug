if (user)
  #{user.name} (#{user.login}) | 
  form(method="post", action=basepath + "/logout")#logoutform
    input(type="hidden", name="_csrf", value=csrfToken)
    button(type="submit")= __i('Logout')
