ui.ready(function () {
  ui.placeholderLabel('login', 'loginlabel');
  ui.placeholderLabel('passwd1', 'passwd1label');
  ui.placeholderLabel('passwd2', 'passwd2label');
  ui.placeholderLabel('realname', 'realnamelabel');
});
